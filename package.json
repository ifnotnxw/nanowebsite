{
  "name": "nanopharm",
  "version": "1.0.0",
  "description": "НаноФарм — медицинское оборудование",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev": "npm start",
    "build": "echo \"Nanopharm: static frontend, nothing to build\"",
    "run": "npm install && npm start",
    "postinstall": "npm install --prefix backend",
    "install:backend": "cd backend && npm install"
  },
  "engines": {
  "node": ">=20"
  }
}
